#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
   int n;
   scanf("%d",&n);
    printf("%0.f %0.f",((5+sqrt(5))/10)*pow(((1+sqrt(5))/2),n-2)+((5-sqrt(5))/10)*pow(((1-sqrt(5))/2),n-2),((5+sqrt(5))/10)*pow(((1+sqrt(5))/2),n-1)+((5-sqrt(5))/10)*pow(((1-sqrt(5))/2),n-1));
    return 0;
}
